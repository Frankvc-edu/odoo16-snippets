# -*- mode: snippet -*-
# name: Odoo method compute
# key: oo_compute
# binding: direct-keybinding
# --
@api.depends("${10:field1}", $25)
def _compute_${30:field}(self):
    for record in self:
        ${40:pass}