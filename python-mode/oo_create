# -*- mode: snippet -*-
# name: Odoo Create
# key: oo_create
# binding: direct-keybinding
# --
@api.model
def create(self, vals):
    new_id = super(${10:class_name}, self).create(vals)
    return new_id